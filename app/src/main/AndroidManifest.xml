<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ceroxlol.remindme">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.BROADCAST_CLOSE_SYSTEM_DIALOGS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/RemindMeTheme">
        <activity
            android:name=".activities.EditSingleAppointmentActivity"
            android:label="Edit appointment"
            android:parentActivityName=".activities.EditAppointmentActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.ceroxlol.remindme.activities.EditAppointmentActivity" />
        </activity>
        <activity
            android:name=".activities.EditAppointmentActivity"
            android:label="All appointments"
            android:parentActivityName=".activities.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.ceroxlol.remindme.activities.MainActivity" />
        </activity>
        <activity android:name=".activities.MainActivity"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.AddNewAppointmentActivity"
            android:label="Add new appointment">
        </activity>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activities.ChooseLocationActivity"
            android:label="@string/title_activity_choose_location">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.ceroxlol.remindme.activities.MainActivity" />
        </activity>
        <activity android:name=".activities.EditLocationActivity" />
        <activity android:name=".activities.EditSingleLocationActivity" />

        <receiver android:name=".receiver.AppointmentActionReceiver" />
    </application>

</manifest>